# G1 抓取导航项目 — Python 依赖
# ====================================
# 使用方法（在虚拟环境中安装）：
#   python3 -m venv ~/g1act_venv
#   source ~/g1act_venv/bin/activate
#   pip install -r requirements.txt
#
# 注意：
#   - ROS2 相关包（rclpy, geometry_msgs 等）由系统 apt 安装，不在此处管理
#   - unitree_sdk2py 需从宇树官方仓库安装，pip 可能找不到
#   - 禁止在主系统 Python 中安装，必须使用虚拟环境

# ==================================================================
# YOLO 目标检测
# ==================================================================
ultralytics>=8.0.0  # YOLOv8/v11 目标检测框架

# ==================================================================
# 计算机视觉
# ==================================================================
opencv-python>=4.5.0    # OpenCV 图像处理
numpy>=1.21.0           # 数值计算基础库

# ==================================================================
# ROS2 图像桥接
# ==================================================================
cv-bridge>=1.0.0  # ROS2 图像消息与 OpenCV 格式互转

# ==================================================================
# 宇树机器人 SDK（需手动安装）
# ==================================================================
# unitree_sdk2py  — 宇树 G1 DDS 通信底层 SDK
# 安装方式: pip install git+https://github.com/unitreerobotics/unitree_sdk2py.git
